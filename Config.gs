/**
 * ====================================================
 * MasterChef/Config.gs
 * גרסה: v1.1 (09/05/2026)
 * סטטוס: ✅ STABLE
 * פרויקט: MasterChef
 *
 * תיאור: כל הקבועים הגלובליים של פרויקט MasterChef
 *        כולל זהות, טריגרים, Sheets, עמודות, AI, ווטסאפ, לוג
 *
 * פונקציות:
 *   — קובץ קבועים בלבד, ללא פונקציות
 * ====================================================
 */

// ============================================================
// 1. זהות הפרויקט — חובה
// ============================================================
var PROJECT_NAME    = 'Master Chef';     // חובה — שם הפרויקט לצורכי לוג והודעות שגיאה
var PROJECT_VERSION = 'v1.1';           // חובה — מספר גרסה לתיעוד
var TIMEZONE        = 'Asia/Jerusalem'; // חובה — אזור זמן לכל חישובי תאריך ושעה

// ============================================================
// 2. טריגרים — שמות, ימים ושעות
// ============================================================

// שמות הפונקציות הרשומות כטריגרים — חובה להתאים לשמות הפונקציות ב-Main.gs
var TRIGGER_NAMES = {
  SUNDAY_SCAN:       'sundayScan',       // חובה — סריקת טרנדים ביום ראשון
  MONDAY_SURVEY:     'mondaySurvey',     // חובה — שליחת סקר ביום שני
  TUESDAY_RESULTS:   'tuesdayResults',   // חובה — קריאת תוצאות ביום שלישי בצהריים
  TUESDAY_MATERIALS: 'tuesdayMaterials'  // חובה — שליחת חומרים ביום שלישי בערב
};

// ימי השבוע הרלוונטיים לטריגרים
var DAYS = {
  SUNDAY:  ScriptApp.WeekDay.SUNDAY,  // חובה — יום ראשון: סריקה
  MONDAY:  ScriptApp.WeekDay.MONDAY,  // חובה — יום שני: סקר
  TUESDAY: ScriptApp.WeekDay.TUESDAY  // חובה — יום שלישי: תוצאות + חומרים
};

// שעות ההפעלה של כל טריגר (שעה שלמה, 24h)
var TRIGGER_HOURS = {
  SUNDAY_SCAN:        0, // חובה — חצות (00:00) — סריקת טרנדים בתחילת השבוע
  MONDAY_SURVEY:     18, // חובה — 18:00 — שליחת סקר בערב שני
  TUESDAY_RESULTS:   12, // חובה — 12:00 — קריאת תוצאות בצהריים שלישי
  TUESDAY_MATERIALS: 18  // חובה — 18:00 — שליחת חומרים בערב שלישי
};

// ============================================================
// 3. גיליון Google Sheets — חובה
// ============================================================
var SPREADSHEET_ID = '1HO51chkrQu7mLD_PUMpfHO-3EyCXoEVy'; // חובה — מזהה הגיליון הראשי של MasterChef
var HEADER_ROW     = 1;                                      // חובה — שורת הכותרת
var DATA_START_ROW = 2;                                      // חובה — שורה ראשונה של נתונים

// שמות הטאבים
var SHEET_MAIN    = 'Results'; // חובה — טאב תוצאות השאלון השבועי
var SHEET_MEMBERS = 'Members'; // חובה — טאב רשימת חברי הקבוצה
var SHEET_LOG     = 'Log';     // מומלץ — טאב לוג פעולות

// ============================================================
// 4. עמודות — Results (1-based)
// ============================================================
var COL_RESULTS_DATE    = 1; // חובה — עמודה A: תאריך הסקר
var COL_RESULTS_TREND   = 2; // חובה — עמודה B: טרנד קולינרי
var COL_RESULTS_WINNER  = 3; // חובה — עמודה C: הזוכה
var COL_RESULTS_RECIPE  = 4; // חובה — עמודה D: קישור למתכון
var COL_RESULTS_SENT_AT = 5; // חובה — עמודה E: זמן שליחת הסקר

// עמודות — Members (1-based)
var COL_MEMBERS_NAME   = 1; // חובה — עמודה A: שם החבר
var COL_MEMBERS_PHONE  = 2; // חובה — עמודה B: מספר טלפון
var COL_MEMBERS_ACTIVE = 3; // חובה — עמודה C: פעיל/לא פעיל

// ============================================================
// 5. ווטסאפ (Green API) — חובה
// הערה: GREEN_API_TOKEN ו-INSTANCE_ID שמורים ב-Script Properties — לא כאן!
// ============================================================
var WHATSAPP_GROUP_ID = '120363334672718195@g.us'; // חובה — מזהה קבוצת ווטסאפ של 4 החברים

// ============================================================
// 6. בינה מלאכותית (Claude API) — חובה
// הערה: CLAUDE_API_KEY שמור ב-Script Properties — לא כאן!
// ============================================================
var CLAUDE_MODEL        = 'claude-sonnet-4-6'; // חובה — מודל קלוד לחיפוש טרנדים קולינריים
var MAX_RUNTIME_SECONDS = 300;                  // חובה — שניות מקסימלי לריצה לפני timeout

// ============================================================
// 7. אימיילים — אופציונלי
// ============================================================
var EMAIL_ADMIN = 'sms4sport@gmail.com'; // אופציונלי — כתובת מנהל לדוחות שגיאה

// ============================================================
// 8. לוג וביצועים — מומלץ
// ============================================================
var LOG_LEVEL        = 'INFO'; // מומלץ — רמת לוג: DEBUG / INFO / WARNING / ERROR
var MAX_ROWS_PER_RUN = 50;     // מומלץ — שורות מקסימלי לעיבוד בריצה אחת

// ============================================================
// 9. ניסיונות חוזרים — מומלץ
// ============================================================
var MAX_RETRIES    = 3;    // מומלץ — ניסיונות חוזרים בכישלון API
var RETRY_SLEEP_MS = 2000; // מומלץ — המתנה במילישניות בין ניסיון לניסיון

// ============================================================
// 10. שמירת מצב ב-Script Properties — אופציונלי
// ============================================================
var PROP_KEY_LAST_SURVEY   = 'MC_LAST_SURVEY_DATE';   // אופציונלי — תאריך שליחת הסקר האחרון
var PROP_KEY_CURRENT_TREND = 'MC_CURRENT_TREND';      // אופציונלי — טרנד שנבחר ביום ראשון
var PROP_KEY_LAST_RUN      = 'MC_LAST_RUN_TIMESTAMP'; // אופציונלי — זמן ריצה אחרונה לאחזקה
